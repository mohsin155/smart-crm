[project]
name = "smart-crm"
version = "0.1.0"
description = "FastAPI Smart CRM application"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.119.0",
    "pydantic-settings>=2.11.0",
    "uvicorn>=0.37.0",
]

[tool.uv]
# Optional: enable preview for upcoming features
preview = true
